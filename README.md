# STM32F401_LCD_GENERIC_DRIVER
This project demonstrates prime number detection using Embedded C on STM32F401. The system checks numbers from 0–100 and displays prime numbers on a 16×2 LCD module using an 8-bit parallel interface. GPIO pins are configured manually without HAL to understand low-level peripheral control.
